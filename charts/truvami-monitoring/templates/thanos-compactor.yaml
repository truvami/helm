{{- if and .Values.thanos.enabled .Values.thanos.compactor.enabled }}
{{- $name := include "truvami-monitoring.thanos.compactor.name" . -}}
{{- $secretName := include "truvami-monitoring.thanos.objstoreSecretName" . -}}
{{- $secretKey := include "truvami-monitoring.thanos.objstoreSecretKey" . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $name }}
  labels: {{- include "truvami-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: thanos-compactor
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 10902
      targetPort: http
      protocol: TCP
  selector: {{- include "truvami-monitoring.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: thanos-compactor
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $name }}
  labels: {{- include "truvami-monitoring.labels" . | nindent 4 }}
    app.kubernetes.io/component: thanos-compactor
spec:
  serviceName: {{ $name }}
  replicas: 1
  selector:
    matchLabels: {{- include "truvami-monitoring.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: thanos-compactor
  template:
    metadata:
      labels: {{- include "truvami-monitoring.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: thanos-compactor
    spec:
      securityContext:
        fsGroup: 65534
        runAsGroup: 65534
        runAsNonRoot: true
        runAsUser: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: thanos
          image: "{{ .Values.thanos.image.repository }}:{{ .Values.thanos.image.tag }}"
          imagePullPolicy: {{ .Values.thanos.image.pullPolicy }}
          args:
            - compact
            - --log.level={{ .Values.thanos.compactor.logLevel }}
            - --log.format=json
            - --http-address=0.0.0.0:10902
            - --data-dir=/var/thanos/compact
            - --objstore.config-file=/etc/thanos/objstore.yml
            - --wait
            {{- with .Values.thanos.compactor.retentionResolutionRaw }}
            - --retention.resolution-raw={{ . }}
            {{- end }}
            {{- with .Values.thanos.compactor.retentionResolution5m }}
            - --retention.resolution-5m={{ . }}
            {{- end }}
            {{- with .Values.thanos.compactor.retentionResolution1h }}
            - --retention.resolution-1h={{ . }}
            {{- end }}
          ports:
            - name: http
              containerPort: 10902
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /-/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources: {{- toYaml .Values.thanos.compactor.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: data
              mountPath: /var/thanos/compact
            - name: objstore
              mountPath: /etc/thanos
              readOnly: true
      volumes:
        - name: objstore
          secret:
            secretName: {{ $secretName }}
            items:
              - key: {{ $secretKey }}
                path: objstore.yml
        {{- if not .Values.thanos.compactor.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
  {{- if .Values.thanos.compactor.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- with .Values.thanos.compactor.persistence.storageClassName }}
        storageClassName: {{ . }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.thanos.compactor.persistence.size }}
  {{- end }}
{{- end }}
