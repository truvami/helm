apiVersion: v1
kind: Service
metadata:
  name: {{ include "truvami-intranet.fullname" . }}
  labels:
    {{- include "truvami-intranet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truvami-intranet.selectorLabels" . | nindent 4 }}
