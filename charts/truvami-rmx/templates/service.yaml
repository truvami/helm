apiVersion: v1
kind: Service
metadata:
  name: {{ include "truvami-rmx.fullname" . }}
  labels:
    {{- include "truvami-rmx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truvami-rmx.selectorLabels" . | nindent 4 }}
