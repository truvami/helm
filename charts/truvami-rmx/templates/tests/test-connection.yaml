apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "truvami-rmx.fullname" . }}-test-connection"
  labels:
    {{- include "truvami-rmx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "truvami-rmx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
