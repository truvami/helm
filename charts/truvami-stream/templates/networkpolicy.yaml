{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "truvami-stream.fullname" . }}
  labels:
    {{- include "truvami-stream.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "truvami-stream.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from: []
      ports:
        - protocol: TCP
          port: {{ .Values.stream.server.port }}
        {{- if .Values.stream.metrics.enabled }}
        - protocol: TCP
          port: {{ .Values.stream.metrics.port }}
        {{- end }}
  egress:
    # Allow DNS resolution
    - to: []
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Allow HTTPS outbound (for JWKS endpoint)
    - to: []
      ports:
        - protocol: TCP
          port: 443
    # Allow HTTP outbound
    - to: []
      ports:
        - protocol: TCP
          port: 80
    # Allow database connections
    - to: []
      ports:
        - protocol: TCP
          port: 5432
    # Allow Valkey/Redis connections
    - to: []
      ports:
        - protocol: TCP
          port: 6379
{{- end }}
