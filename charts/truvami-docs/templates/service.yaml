apiVersion: v1
kind: Service
metadata:
  name: {{ include "truvami-docs.fullname" . }}
  labels:
    {{- include "truvami-docs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "truvami-docs.selectorLabels" . | nindent 4 }}
