apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "truvami-docs.fullname" . }}-test-connection"
  labels:
    {{- include "truvami-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "truvami-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
